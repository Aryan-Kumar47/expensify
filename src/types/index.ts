export type TripType = {
    id: string ;
    userId : string;
    place: string;
    country: string
}
export type ExpenseType = {
    id: string;
    title: string;
    amount: number;
    category: string;
}
